# 改檔前備份（見 CLAUDE.md 硬規則 2）：有了 git history 之後不需要再進 commit
*.bak
*.bak-*

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/

# 虛擬環境
.venv/
venv/
env/

# PyInstaller 打包產物（build/ 是中間分析檔，dist/ 是最終 exe）。
# 兩者都不進 git 歷史——二進位 exe 每次重新打包都會整包改變，commit 進版控
# 只會讓 repo 越滾越大。發布用 GitHub Releases 附加 exe 檔，不用 git 追蹤，
# 見 README「下載與安裝」。已於 2026-07-05 完成煙霧測試驗收，見
# docs/build-feasibility.md、ops/lessons.md L-007~L-010。
build/
dist/

# 編輯器 / 作業系統雜訊
.vscode/
.idea/
Thumbs.db
.DS_Store

# 記憶體/健康檢測跑出來的暫存報告或 log（若日後落地成檔案）
*.log
